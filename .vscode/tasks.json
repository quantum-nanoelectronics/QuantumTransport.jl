{
    "version": "2.0.0",
    "tasks": [
        {
            "label": "Run Driver",  
            "type": "shell",        
            "command": "julia",     
            "args": [
                "--project=${workspaceFolder}",    
                "${workspaceFolder}/test/TestDriver.jl"  
            ],
            "problemMatcher": [],
            "group": {
                "kind": "build",
                "isDefault": true
            },
            "detail": "Run the Julia Driver script using the launch configuration"
        }
    ]
}